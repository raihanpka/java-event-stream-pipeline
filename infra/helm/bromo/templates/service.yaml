apiVersion: v1
kind: Service
metadata:
  name: {{ include "bromo.fullname" . }}
  labels:
    {{- include "bromo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bromo.selectorLabels" . | nindent 4 }}
